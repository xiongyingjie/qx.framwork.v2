@model Web.ViewModels.Report.Edit_M
@{
    Layout = ViewData["Layout"].ToString();
}

                    @Html.HiddenFor(o => o.ReportID)

                    @Html.InputFor(o => o.ReportName)

                    <div class="form-group">
                        @Html.LabelFor(o => o.SqlStr, new { @class = "col-md-3 control-label" })
                        <div class="col-md-9">
                            @Html.TextAreaFor(o => o.SqlStr, new { @class = "form-control" })
                        </div>
                    </div>

                    @Html.InputFor(o => o.HeadFields)

                    @Html.InputFor(o => o.RecordsPerPage)

                    @Html.InputFor(o => o.ParaNames)

                    @Html.InputFor(o => o.ColunmToShow)

                    <div class="form-group">
                         @Html.LabelFor(o => o.OperateColum, new { @class = "col-md-3 control-label" })
                         <div class="col-md-9">
                              @Html.TextAreaFor(o => o.OperateColum, new { @class = "form-control" })
                         </div>
                   </div>

