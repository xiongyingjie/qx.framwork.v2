@model Web.Areas.Contents.ViewModels.CTD_M
@{
    Layout = ViewData["Layout"].ToString();
}
@Html.InputFor(o => o.TableName)

@Html.HiddenFor(o => o.CT_ID)

@Html.HiddenFor(o => o.CTD_ID)

@Html.HiddenFor(o => o.typename)